//未完成 
#include<stdio.h>
#include<string.h>
#define N 20
int main()
{
	char ch[N];
	char c;  //保存前置运算符 
	int x,y;
	int i=1;
	gets(ch);
	c=[0];
	while(ch[i++]!='\n' && i<N)
	{
		if(ch[i-1]=='\0')
			i+=1;
		else
			x=	
			
	}
	return 0;
} 
